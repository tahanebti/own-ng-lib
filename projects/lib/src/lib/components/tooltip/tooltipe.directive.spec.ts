import { TooltipeDirective } from './tooltipe.directive';

describe('TooltipeDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipeDirective();
    expect(directive).toBeTruthy();
  });
});
